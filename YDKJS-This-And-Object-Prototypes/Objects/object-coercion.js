/*
*	Use the literal form for a value. The language automatically coerces a primitive to a object when necessary.
*/

var strPrimitive = "I am a string";

console.log( strPrimitive.length );	// 13

console.log( strPrimitive.charAt( 3 ) );	// "m"

