/*
*	JS doesn't how specific values/structures are displayed to the user, these are handled by the browser. As such, each browser will handle these a little differently
*/

function Foo() {

}

var a1 = new Foo();

console.log(a1);	// Foo()